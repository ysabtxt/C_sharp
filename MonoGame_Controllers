using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using Microsoft.Xna.Framework.Input;
using System;

namespace Input_Project
{
    public class Game1 : Game
    {
        private GraphicsDeviceManager _graphics;
        private SpriteBatch _spriteBatch;
        Texture2D ballTexture, boxTexture;
        Rectangle ballRectangle, boxRectangle;
        Color ballColor, boxColor;
        Random rand;
        public Game1()
        {
            _graphics = new GraphicsDeviceManager(this);
            Content.RootDirectory = "Content";
            IsMouseVisible = true;
        }

        protected override void Initialize()
        {
            ballRectangle = new Rectangle(10,10,100,100);
            boxRectangle = new Rectangle(200,200,100,100);
            ballColor = Color.White;
            boxColor = Color.White;
            rand = new Random();
            base.Initialize();
        }

        protected override void LoadContent()
        {
            _spriteBatch = new SpriteBatch(GraphicsDevice);
            ballTexture = Content.Load<Texture2D>("Ball");
            boxTexture = Content.Load<Texture2D>("Box");
            
        }// IskeyUp() mean key is currntly not pressed

        // rand.Next(0,256) => r = 200, g = 50, b = 150 : purpleishg color
        protected override void Update(GameTime gameTime)
        {
            #region Keyboard

            // when d is pressed down, box moves to right
            //if (Keyboard.GetState().IsKeyDown(Keys.D))
            //{
            //    boxRectangle.X += 1;
            //}

            // if U is not pressed, it changes color
            //if (Keyboard.GetState().IsKeyUp(Keys.U))
            //{
            //    ballColor = new Color(rand.Next(0,256), rand.Next(0, 256), rand.Next(0, 256));
            //}

            //returns: D W SPACE
            //Keys[] key = Keyboard.GetState().GetPressedKeys();

            //Looks at every pressed keys
            //foreach (Keys k in key)
            //{
            //      if any key other than d is pressed, the box is getting bigger
            //    if (k != Keys.D)
            //    {
            //        boxRectangle.Size += new Point(1, 1); 
            //    }
            //}
            #endregion

            #region Mouse
            // onbly move the ball while the left mouse is held
            //if (Mouse.GetState().LeftButton == ButtonState.Pressed)
            //{
            //    ballRectangle.Location = Mouse.GetState().Position;
            //}
            #endregion

            #region GamePad
            // move left
            //if(GamePad.GetState(PlayerIndex.One).ThumbSticks.Left.X < 0)
            //{
            //    boxRectangle.X -= 1;

            //moves right
            //} else  if (GamePad.GetState(PlayerIndex.One).ThumbSticks.Left.X > 0)
            //{
            //    boxRectangle.X += 1;
            //}

            // vertical movement
            //if (GamePad.GetState(PlayerIndex.One).ThumbSticks.Right.Y < 0)
            //{ moves down
            //    boxRectangle.Y += 1;
            //}
            //else if (GamePad.GetState(PlayerIndex.One).ThumbSticks.Right.Y > 0)
            //{ moves up
            //    boxRectangle.Y -= 1;
            //}

            // when pressed x, ball changes colors
            //if(GamePad.GetState(PlayerIndex.One).Buttons.X == ButtonState.Pressed)
            //{
            //    ballColor = new Color(rand.Next(0, 256), rand.Next(0, 256), rand.Next(0, 256));
            //}

            // when pressed y, ball changes colors
            //if (GamePad.GetState(PlayerIndex.One).IsButtonDown(Buttons.Y))
            //{
            //    ballColor = new Color(rand.Next(0, 256), rand.Next(0, 256), rand.Next(0, 256));
            //}

            // Gamepad: ball moves up
            //if(GamePad.GetState(PlayerIndex.One).DPad.Up == ButtonState.Pressed)
            //{
            //    ballRectangle.Y -= 1;
            //}

            // Gamepad: ball moves down
            //if (GamePad.GetState(PlayerIndex.One).DPad.Down == ButtonState.Pressed)
            //{
            //    ballRectangle.Y += 1;
            //}

            #endregion

            base.Update(gameTime);
        }

        protected override void Draw(GameTime gameTime)
        {
            GraphicsDevice.Clear(Color.CornflowerBlue);
            _spriteBatch.Begin();
                _spriteBatch.Draw(boxTexture, boxRectangle,boxColor);
                _spriteBatch.Draw(ballTexture, ballRectangle,ballColor);
            _spriteBatch.End();
            base.Draw(gameTime);
        }
    }
}
